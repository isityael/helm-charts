fail2ban-gotify-relay has been installed.

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Required configuration:
  - gotifyURL must point at your Gotify server.
  - existingSecret should reference a Secret containing GOTIFY_TOKEN.

Check rollout status:
  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "fail2ban-gotify-relay.fullname" . }}

{{- if .Values.ingress.enabled }}
Ingress is enabled. Check the generated Ingress:
  kubectl -n {{ .Release.Namespace }} get ingress {{ include "fail2ban-gotify-relay.fullname" . }}
{{- else if .Values.httpRoute.enabled }}
HTTPRoute is enabled. Check the generated route:
  kubectl -n {{ .Release.Namespace }} get httproute {{ include "fail2ban-gotify-relay.fullname" . }}
{{- else }}
No external route is enabled. Port-forward locally:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "fail2ban-gotify-relay.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}
