apiVersion: v1
kind: Service
metadata:
  name: {{ include "fail2ban-gotify-relay.fullname" . }}
  labels:
    {{- include "fail2ban-gotify-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fail2ban-gotify-relay.selectorLabels" . | nindent 4 }}
