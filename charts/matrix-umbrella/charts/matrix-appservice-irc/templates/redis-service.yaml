{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "matrix-appservice-irc.redisFullname" . }}
  labels:
    {{- include "matrix-appservice-irc.componentLabels" (dict "context" . "component" "redis") | nindent 4 }}
spec:
  selector:
    {{- include "matrix-appservice-irc.componentSelectorLabels" (dict "context" . "component" "redis") | nindent 4 }}
  ports:
    - name: redis
      protocol: TCP
      port: {{ .Values.redis.service.port }}
      targetPort: redis
{{- end }}
