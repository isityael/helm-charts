#      .
#     / V\
#   / `  /
#  <<   |    External Redis/Postgres example for matrix-appservice-irc.
#  /    |    Disable bundled services and provide explicit connection fields.
#

host: irc-media.example.com

homeserver:
  url: http://ess-synapse.ess.svc.cluster.local:8008
  domain: matrix.example.com

postgres:
  enabled: false

redis:
  enabled: false
  url: redis://redis.example.com:6379/0

database:
  postgres:
    host: postgres.example.com
    port: 5432
    user: matrix_irc
    password:
      value: replace_me
    database: matrix_irc
    sslMode: prefer
