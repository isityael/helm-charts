#      .
#     / V\
#   / `  /
#  <<   |    Self-Signed / Custom TLS example for matrix-appservice-irc.
#  /    |    You should probably merge into `values.matrix.example.yaml`
#

host: irc-media.example.com

homeserver:
  url: http://ess-synapse.ess.svc.cluster.local:8008
  domain: matrix.example.com

ingress:
  clusterIssuer: ""
  tls:
    enabled: true
    # Load your cert and key into a Kubernetes TLS secret, then reference it here.
    # Example:
    # kubectl -n <namespace> create secret tls matrix-appservice-irc-media-proxy-tls --cert=tls.crt --key=tls.key
    secretName: matrix-appservice-irc-media-proxy-tls
