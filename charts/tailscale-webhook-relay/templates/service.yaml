apiVersion: v1
kind: Service
metadata:
  name: {{ include "tailscale-webhook-relay.fullname" . }}
  labels:
    {{- include "tailscale-webhook-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tailscale-webhook-relay.selectorLabels" . | nindent 4 }}
