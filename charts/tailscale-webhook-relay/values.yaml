# Default values for tailscale-webhook-relay.

image:
  repository: ghcr.io/yaelmoshi/tailscale-webhook-relay
  pullPolicy: IfNotPresent
  tag: "4cb886bb@sha256:382dacc9dca15ad3a86f4661a83b4912c1f39f28789b70615542c0a58a57653c"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# -- ntfy target URL (e.g. https://ntfy.m0sh1.cc/tailscale-events)
ntfyURL: ""

# -- Existing secret containing TAILSCALE_WEBHOOK_SECRET and NTFY_TOKEN keys
existingSecret: ""

serviceAccount:
  create: true
  automount: false
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext:
  fsGroup: 65534

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 65534
  runAsGroup: 65534

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: tailscale-webhook.example.com
      paths:
        - path: /
          pathType: Prefix
  tls: []

httpRoute:
  enabled: false
  annotations: {}
  parentRefs: []
  hostnames: []

resources:
  requests:
    cpu: 5m
    memory: 16Mi
  limits:
    memory: 32Mi

livenessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 30

readinessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 3
  periodSeconds: 10

nodeSelector:
  kubernetes.io/os: linux

tolerations: []
affinity: {}
