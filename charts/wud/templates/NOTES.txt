Thank you for installing WUD (What's Up Docker)!

{{- if .Values.ingress.enabled }}
1. Open your browser at the configured ingress host(s).
{{- else }}
1. Port-forward the service:
   kubectl port-forward svc/{{ include "wud.fullname" . }} 8080:{{ .Values.service.port }}
2. Open http://127.0.0.1:8080
{{- end }}
