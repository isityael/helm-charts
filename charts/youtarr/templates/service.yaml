apiVersion: v1
kind: Service
metadata:
  name: {{ include "youtarr.fullname" . }}
  labels:
    {{- include "youtarr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "youtarr.selectorLabels" . | nindent 4 }}
